# Model outputs / artifacts (kept local; not published)
fine_tune/
fine_tuned_params_v1/
fine_tuning.py

# Research-only folders
outreach/
paper/

# Local assistant state
.claude/
Claude outputs/

# Tests: only force-added files are tracked (git add -f tests/<file>)
tests/

# Python caches
__pycache__/
*.py[cod]
.pytest_cache/
.mypy_cache/
.ruff_cache/
.cache/

# Jupyter checkpoints
.ipynb_checkpoints/

# Local environments and secrets
.env
.env.*
.venv/
venv/
env/

# Model weights and training checkpoints
*.safetensors
*.bin
*.pt
*.pth
*.ckpt
checkpoints/
qwen3-4b-local/

# Per-run outputs (main.py / notebook)
hive_energy_trace.jsonl
hive_memory/
hive_answer.txt
